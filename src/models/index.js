import User from './userModel';
import Post from './postModel';
import Permission from './permissionModel';
import Role from './roleModel';

const models = { User, Post, Permission, Role };
export default models;
